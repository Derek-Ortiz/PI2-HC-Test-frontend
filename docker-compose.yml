services:
  frontend: 
    image: derekortiz/hc-frontend
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      NEXT_URL: ${NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev

volumes:
  frontend-data: